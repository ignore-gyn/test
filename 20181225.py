param = {}

disp_format = "%7.2f"



param["max"] = (disp_format % field_max).strip()
param["min"] = 

param["disp_value"] = 
value_list = param["disp_value"].split(",")

map(lambda x: s.strip('" '), s.split(","))

param["flg_sens"]
param["flg_sels"]
